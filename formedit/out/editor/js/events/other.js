
document.addEventListener('scroll', function (event) {

    //contextmenu
    contextmenu.hide();

    /*
    if (event.target.id==="desktopborder") { // or any other filtering condition
    }
    */
}, true /*Capture event*/);